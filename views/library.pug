extends layout.pug
    
block content    
    a(onclick='goBack()', class = 'back-button button') < Back to dashboard

    h1 Your entire music library, (Re)Wrapped! ✨

    h2 Rediscover the overlooked tunes of your Spotify library.

    div
        if lowest && lowest.length > 0
            table 
                each t in lowest
                    tr 
                    - var artist_name = t.artists[0].name
                    - var track_name = t.name
                    - var track_preview = t.preview_url

                    td #{track_name} - #{artist_name}
                    td: a(href=track_preview) Preview
        else 
            p.empty-library It’s unusually quiet in here... Looks like there are no tracks yet! It's the perfect moment to start adding some. 

    //////////////
    // SCRIPTS //
    script.
        function goBack() {
            window.history.back();
        }