extends layout.pug
    
block title
  title Add Playlist to Library - Spotify (Re)Wrapped

block gtag
  // Google tag (gtag.js)
  script(async src="https://www.googletagmanager.com/gtag/js?id=G-KEDM3SKHDJ")
  script.
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-KEDM3SKHDJ');

block content
    ////////////  
    // STYLE //
    style.
        img {
            width: 200px;
            height: 200px;
            margin-bottom: 20px;
        }
        a:visited {
            color: white;
            text-decoration: none;
        }

    //////////////
    // CONTENT //
    
    // a(href='/dashboard' class='back-button button') < Back to dashboard
    a(href=`/dashboard?userid=${userid}`, class = 'back-button button') < Back to dashboard

    br

    if playlist.images && playlist.images.length > 0
        img(src=playlist.images[0].url, class='playlist_image')
    else
        img(src="https://www.solidbackgrounds.com/images/1920x1080/1920x1080-gray-solid-color-background.jpg", class='playlist_image')
    
    h1 Rewrapping your playlist "#{playlist.name}"!

    h3 Hang tight before you immediately hit that open button! It takes a hot minute for us to whip up your playlist with a sprinkle of magic and a dash of flair, so give it a sec or two — good things come to those who wait! 🎶

    a(href=playlist.uri class="create-playlist-button button") Open in Spotify

    p 
    //////////////